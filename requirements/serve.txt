# Inference-only dependencies - every version pinned exactly.
# No tensorboard, no training utilities.
#
# Install from PyTorch's CPU wheel index (see requirements/train.txt for why):
#   pip install --index-url https://download.pytorch.org/whl/cpu \
#               --extra-index-url https://pypi.org/simple -r serve.txt
torch==2.11.0
torchvision==0.26.0
numpy==2.4.2
pillow==12.1.1
PyYAML==6.0.3

fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
